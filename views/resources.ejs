<h1>Resources</h1>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Provider</th>
    </tr>
  </thead>
  <tbody>
    <!-- TODO: Start resources.forEach loop -->
    <% resources.forEach(function(resource) { %>
    <tr>
      <td><!-- TODO: Replace with resource.name --><%= resource.name %></td>
      <td><!-- TODO: Replace with resource.provider --><%= resource.provider %></td>
    </tr>
    <!-- TODO: End resources.forEach loop -->
    <% }); %>
  </tbody>
</table>